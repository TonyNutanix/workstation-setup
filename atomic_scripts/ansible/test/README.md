# Testing
This single file can be used to check if ansible can connect to the Prism Central Instance
1. Update the file to have the correct IP, username, and password
2. Run 'ansible-playbook cluster_info.yaml'