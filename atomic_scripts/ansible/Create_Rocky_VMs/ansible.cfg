[defaults]
# Disable host key checking for localhost connections
host_key_checking = False

# Reduce noise in output
deprecation_warnings = False
retry_files_enabled = False

# Increase timeout for Nutanix API calls (image uploads can be slow)
timeout = 300

# Use YAML callback for cleaner output
stdout_callback = yaml

[inventory]
enable_plugins = auto
